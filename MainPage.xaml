<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="LabelStyleFontIssue.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    NavigationPage.HasNavigationBar="False"
    Shell.NavBarIsVisible="False">
    <ScrollView>
        <VerticalStackLayout Padding="30,0" Spacing="25">
            <Label Style="{StaticResource SubHeadline}" Text="MainPage" />
            <Image
                Aspect="AspectFit"
                HeightRequest="185"
                Source="dotnet_bot.png" />
            <Label Style="{StaticResource SubHeadline}" Text="abcdefghijklmnopqrstuvwxyz" />
            <Label Style="{StaticResource SubHeadline}" Text="ABCDEFGHIJKLMNOPQRSTUVWXYZ" />
            <Label Style="{StaticResource SubHeadline}" Text="!#$%'()*+,-./:;=?@[\]^_`{|}~" />
            <Button
                x:Name="CounterBtn"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill"
                Text="Go to StyledPage" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
